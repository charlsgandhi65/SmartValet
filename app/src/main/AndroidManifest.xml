<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartValet"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Launcher Activity (Splash) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Welcome Activity (not launcher) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <activity android:name=".SignUpActivity" />

        <!-- Admin Activities -->
        <activity android:name=".AdminActivity" />
        <activity android:name=".ViewEventsActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".EditEventActivity" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".EventsHistoryActivity" />
        <activity android:name=".ManageStaffActivity" />
        <activity android:name=".AddStaffToEventActivity" />

        <!-- Customer Activities -->
        <activity android:name=".CustomerActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ViewAvailableEventsActivity" />
        <activity android:name=".BookParkingActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".BookingConfirmationActivity" />
        <activity android:name=".MyBookingsActivity" />
        <activity android:name=".ValetRequestActivity" />
        <activity android:name=".MyValetRequestsActivity" />
        <activity android:name=".ValetVehicleEntryActivity" />
        <activity android:name=".ValetPaymentSuccessActivity" />

        <!-- Staff Activities -->
        <activity android:name=".StaffActivity" />
        <activity android:name=".StaffViewEventsActivity" />
        <activity android:name=".QRScannerActivity" />
        <activity android:name=".ViewRequestsActivity" />
        <activity android:name=".ManageVehiclesActivity" />

        <!-- ZXing Camera Capture Activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />

    </application>

</manifest>
